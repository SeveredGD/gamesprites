# Everdeep Modular Relic Reservoir — Production

Production-ready replacement for the compact relic sip pips.

Start with `IMPLEMENTATION.md`. The folder includes:

- transparent V4 modules at seven real production heights;
- standalone CSS and JavaScript renderer;
- interactive `demo.html`;
- `manifest.json` with state and assembly rules;
- `size-tests.png` showing the true rendered sizes.

The complete generated bar must never be stretched. Assemble left + repeating centers + right, with zero gap and zero overlap.
